import React from 'react'

const APILoadingStates = () => {
  return (
    <div className="blog">
      <h2>How to display loading state during API Calls</h2>
      <p>
      We can do it using the React useState hook, which allows us to track the state of a functional component. If you are interested to learn more about them, check out our previous guide about consuming an API in React using React Hooks.
      </p>
    </div>
  )
}

export default APILoadingStates