import React from "react";

const NestedRoutes = () => {
  return (
    <div className="blog">
      <h2>What is Nested routing in react JS?</h2>
      <p>
        Nested Routes are a powerful feature. While most people think React
        Router only routes a user from page to page, it also allows one to
        exchange specific fragments of the view based on the current route.
      </p>
    </div>
  );
};

export default NestedRoutes;
